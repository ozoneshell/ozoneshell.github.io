// include requirements

// ozone class

// check if vfs already exists, if so keep sw alive

// initialize with default apps provider and default apps

// keep sw alive helper

// erase everything helper